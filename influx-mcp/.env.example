# --- MCP InfluxDB Server Configuration ---

# -- General Settings --
# InfluxDB version. Can be '1', '2', or 'auto' (default).
# 'auto' will try to detect the version by pinging v2 and then v1 endpoints.
INFLUX_VERSION=auto

# URL of the InfluxDB instance.
# For v2, e.g., https://us-east-1-1.aws.cloud2.influxdata.com
# For v1, e.g., http://localhost:8086
INFLUX_URL=

# Request timeout in seconds.
INFLUX_REQUEST_TIMEOUT_SEC=30

# -- InfluxDB v2 Settings (required if INFLUX_VERSION is '2' or 'auto' on a v2 instance) --
# The organization name in InfluxDB Cloud or v2 OSS.
INFLUX_ORG=
# The API token. This is a secret and should be handled securely.
INFLUX_TOKEN=
# Optional: A default bucket to use if not specified in a tool call.
INFLUX_DEFAULT_BUCKET=

# -- InfluxDB v1.x Settings (required if INFLUX_VERSION is '1' or 'auto' on a v1 instance) --
# Username for authentication. Can be left blank if auth is disabled.
INFLUX_USERNAME=
# Password for authentication. This is a secret.
INFLUX_PASSWORD=
# Optional: A default database to use if not specified in a tool call.
INFLUX_DEFAULT_DB=
# Optional: A default retention policy to use.
INFLUX_DEFAULT_RP=

# -- MCP Server Settings --
# Log level for the server. Can be DEBUG, INFO, WARNING, ERROR.
MCP_LOG_LEVEL=INFO
